Sometimes strange segmentation faults ending in libncurses can be seen.

vifm doesn't always resize to fit terminal window.

Strange piece of escape sequence 37m

Selection isn't cleared on keys in normal mode.
