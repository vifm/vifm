language: c

script:
 - sudo pip install cpp-coveralls
 - autoreconf && ./configure --enable-coverage && make -j4 && make check CC="$CC"

compiler:
 - gcc
 - clang

after_success:
 - coveralls --encoding iso-8859-1 --build-root src/ --gcov-options '\-p'

notifications:
  email:
    recipients:
      - xaizek@openmailbox.org
