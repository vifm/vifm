#ifndef __MODES_H__
#define __MODES_H__

enum
{
	NORMAL_MODE,
	CMDLINE_MODE,
	VISUAL_MODE,
	MODES_COUNT
};

void init_modes(void);

#endif

/* vim: set tabstop=2 softtabstop=2 shiftwidth=2 noexpandtab : */
